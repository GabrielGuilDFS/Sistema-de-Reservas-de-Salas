<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Name, "Nome", style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :room_type, "Tipo da Sala", style: "display: block" %>
    <%= form.text_field :room_type %>
  </div>

  <div>
    <%= form.label :capacity, "Capacidade", style: "display: block" %>
    <%= form.number_field :capacity %>
  </div>

  <div>
    <%= form.label :resources, "Recursos", style: "display: block" %>
    <%= form.text_field :resources %>
  </div>
  
  <br>

  <div>
    <%= form.submit "Salvar Sala", class: "btn btn-primary" %>
  </div>
<% end %>
